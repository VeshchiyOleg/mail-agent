target/
*.class
*.jar
!.mvn/wrapper/*.jar
dependency-reduced-pom.xml

.idea/
*.iml
.vscode/

.DS_Store

# local runtime data / secrets — never commit these
data/
logs/
*.local.yaml
.env
config.yaml

# not project files — Claude Code setup materials that landed in this dir,
# must never end up in the submitted repo
claude-code-setup.md
claude-code-setup.pdf
